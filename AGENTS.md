# AGENTS.md — open-farm 编码 Agent 上手说明

> 只写代码 / 测试 / 配置里读不出来的约束与坑。其余指向源头：玩法 `README.md`，
> 数值 `data/**/*.tres`，规则 `tests/`，单点设计读脚本顶部 `##`，历史 `git log`。

## 铁律（测试兜不住，靠自觉）

0. **禁止直接读取整个文件**：先按 `## 定位` 取一条，取到就动手。
1. **只加数据，不改结构**：内容进 `res://data/**/*.tres`，脚本只认 id；`XxxData`（`Resource`）↔ `XxxState`（`RefCounted`），规则写成纯静态函数。
2. **不再新增 Autoload**：全局只留跨域时间 / 场景 / 存档信号，领域信号走 `EventBus.player/farm/world/ui`；其余状态与服务挂 `Main` 组合根注入。
3. **日结转按显式顺序**：`GameDateClock.register_day_hook(callable, DayPipeline.PRIORITY_*)`，在 `_exit_tree` 注销；不依赖信号回调顺序。
4. **缓存复用的世界场景不重跑 `_ready()`**：每次进图逻辑放 `_enter_tree()` / `WorldScene.on_world_enter()`。
5. **生成物只改生成器**：`assets/**` 永不手改，改 `tools/` 后重跑 `./tools/build_assets.sh`；确定性判据是连跑两次 `git status` 干净。
6. **测试输出收口**：Godot 输出重定向到 `.tmp/check/*.log`，控制台只回统计与失败明细。
7. **禁止反复验证**：只有逻辑或生成物变了才跑测试，且同一批改动只跑一次对应的那条命令；纯布局 / 配色 / 注释调整一律不重跑，不要用"再跑一次确认"代替判断。

## 归属（第一原则，动手前先过一遍）

**每一份状态、每一条行为、每一个副作用（声音 / 画面 / 界面）都有且只有一个主人**——它所属的那个域。主人负责它的创建、持有、修改、销毁；其余人只能通过接口**请求**，不能直接触碰。

三条推论：**单一权威**（状态只有一处真相，其余都是投影 / 缓存）；**行为贴着数据走**（谁的数据，谁的操作）；**依赖显式交给主人**（装配点注入，而不是主人自己去全局找）。

唯一允许无主的，是**真正跨域、没有单一归属者**的信号与生命周期：时间、场景、存档。只有它们配进全局集市。

### 四问判据

1. 它是谁的状态 / 行为？——答不上来，就是无主信号，或一处设计缺口。
2. 改它要不要绕过谁？——需要越过某个对象私有边界的，就是错位。
3. 它从哪儿拿到协作者？——来自全局查找 / 全局访问点的，依赖方向就是反的。
4. 它住在哪个宿主里？——宿主不是它那个域的，就是挂错了层。

### 症状 → 根因 → 处方

| 症状 | 根因 | 处方 |
| --- | --- | --- |
| 一个节点订阅面横跨数域，还要用开关 / 导出字段配置"订阅哪些" | **依赖过宽** | 拆成按域的小对象，各自持有被注入的依赖 |
| 结算 / 发现 / 判定写在界面或组合根 | **职责错层** | 行为搬回数据的域；界面与组合根只发请求、只做装配 |
| 一个自洽的玩法被拆到规则 / 状态 / 视图 / 界面 / 音频 | **内聚不足** | 收成一个单元；单元内部可自由分层，对外只留接口 |
| 全局单例扫描全场收集状态 | **生命周期无主** | 宿主显式登记自己持有的对象；登记即归属 |
| 领域对象挂在全局对象上供人取用 | **依赖方向反了** | 由装配点注入；全局只留无主信号 |

后四类常与"依赖过宽"同时出现，但根因不同、处方也不同。**不要用"再拆一个中心对象"去治依赖过宽**——那只会得到第二个中心。

### 天然共享点：允许，但要过三关

少数东西天然跨域（会被临时接管的 BGM、"刚响过一声"的抑制时间戳、全局音量）。它们可以存在，但必须同时满足 **极小 / 有主 / 显式**：只承载共享语义、不承载任何域的规则；哪怕共享也指名一个所有者；跨域访问走接口，且能一句话说清它为什么共享。

**两者不可兼得时，优先接受行为退化**，不要为了保留行为重新引入中心节点。出现"配置订阅哪些"的开关，就是这个共享点正在膨胀，拆回各域。

### 已定的通用边界（改到这些地方先看这里）

- **取世界上下文**：属于某张地图的东西（农田 / 植被 / 水面 / 日程点）在**自己所属的世界场景内**解析；跨世界 / 会话级的东西（当前背包、当前钓鱼单元、当前物品栏）由组合根治提供者注入。两者都禁止全树分组查找。
- **进世界**："进入一张地图"是一个显式生命周期事件；地图自身的表现（音乐 / 光照 / 季节外观）在该事件上恢复。不各自造触发点，不为广播新增全局单例，也不用"重建场景"换简单。
- **可存档状态**：宿主显式登记自己持有的对象，**登记动作即声明归属**，不靠扫描全场发现。存档是关键路径，改动单独评估，不与其他域混做。
- **声音**：谁制造声音，谁持有播放器并直接播放；BGM 归地图（曲目由地图声明，昼夜换曲与临时接管由它的播放器处理，缓存里的旧地图不发声）。共享的只有总线与音量这类纯数据。
- **界面**：只展示与发请求，不碰背包 / 金钱 / 结算，数值仍归数据；界面只读状态，不接管存档。
- **模式锁壳**：通用状态机只留"锁移动 / 转输入 / 切回待机"，规则归对应单元（钓鱼就是这个形态）。
- **谁的状态谁显示**：常驻界面只是容器，各域状态由各自的小视图订阅自己域的信号渲染；容器不镜像别人的状态，也不替谁保存一份。
- **复用而非自造接口**：域可以有意识地依赖世界侧的基类（例如矿石复用"伪植被"那套接口），但不反向接管对方。

### 排序与门禁

先做**耦合低、外部依赖少、可独立验证**的域；能被别的域当样板复用的尽早做。
**前置门禁**：先收缩全局对象上的领域访问点，再谈各域"零全局依赖"——否则每个子任务做完都会被后门重新污染。

### 决策记在哪

**跨域 / 不写下来就会反复争论的决策，写进本节**（一句一行也可以）；单点设计写脚本顶部的 `##`。
一条决策写清四件事：**主张、理由、边界（不做什么）、失效条件（何时该重开）**。分工：`AGENTS.md` 说"怎么干活 + 为什么这么设计"，代码与 `##` 说"现状是怎么写的"。

## 定位（缺什么信息，取哪条命令）

跨文件找符号 / 定义 / 影响面 → `codegraph search | def | impact <名>`；
本文件的结构 / 函数体 / 文本 / 调用方 → `python3 tools/outline.py outline | sym | grep | callers`。

一次只取最便宜的那条，取到就动手；实现细节才 `read`（带 `offset/limit`），永不整读。

## 任务路由

| 任务 | 入口 |
| --- | --- |
| 加物品 | `data/items/`（全游戏可持有物品的**总表**；图标 = `assets/sprites/items/<id>.png`）|
| 加对象 / 系统内容 | 同名 `data/<域>/` + `tools/art/generate_<域>.gd`，重跑 `build_assets.sh` |
| 上架 / 定价 | `data/shops/` |
| 加 / 改对话 | 每位 NPC 一个 `data/dialogue/<npc_id>/`（四季问候 + 恋爱），共享的节日 / 事件放 `data/dialogue/shared/`；生成器 `tools/sample/build_dialogues.gd`，文案进 `assets/i18n/dialogue.csv` |
| 加文案 / 汉字 | `assets/i18n/*.csv` |
| 加地图 | `SceneDoor` / `SpawnPoint` + `test_world_map.gd` 的 `MAPS` |
| 改数值 | `data/**/*.tres` |
| 审阅内容 | `./tools/build_wiki.sh --open`（Godot 导出 `.tmp/wiki/data.json` → `build_wiki.py` 渲染静态 HTML）|

## Godot 坑（4.7.2 实测，代码里没有）

- 命令必须自己能退出：套 `timeout` + 末尾 `--quit-after 3`；输出先重定向到文件再 `grep`。
- `-s script.gd` 在 autoload 注册前编译：只能用 `preload()`，依赖 autoload 的工具做成场景。
- 手写 `.tscn`：导出节点引用要 `node_paths=PackedStringArray(...)`，`%UniqueName` 要 `unique_name_in_owner = true`。
- 每张画布只允许一个 `CanvasModulate`；子节点 `_ready()` 先于父节点，初始状态切换用 `call_deferred()`。
- `user://` 可能不可写，存档 / 音频要静默降级；`.godot/` 不入库，新 clone 先 `--import`。
- `PackedFloat32Array` 传参可原地改；`PackedByteArray.encode_u32/_u16/_s16` 可手写二进制；`AudioStreamWAV` 默认 QOA；`ItemList` focus 样式只能画边。

## 测试 / 大文件 / 提交

- 跑 `./tools/check.sh [unit|smoke]`；规范断言在 `tests/unit/test_{assets,audio,i18n,world_map}.gd`。
- 大文件阈值 400 / 800（文件）、80 / 120（函数）：`python3 tools/outline.py lint`。
- 提交 `feat:` / `fix:` / `chore:` 单行中文；生成物与代码一起提交。
